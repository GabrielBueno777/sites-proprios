# Variáveis de Ambiente - FitPlus

# Porta do servidor (padrão: 3000)
PORT=3000

# Ambiente (development, production)
NODE_ENV=development

# CORS Origin (deixe comentado para aceitar todos)
# CORS_ORIGIN=http://localhost:3000
